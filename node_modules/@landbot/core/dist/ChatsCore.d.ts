import { ChatsCoreConfig, Messages } from './types';
export declare class ChatsCore {
    config: ChatsCoreConfig;
    database: Database;
    constructor(config: ChatsCoreConfig);
    init(): Promise<any>;
    destroy(): Promise<void>;
    setChat(user_uuid: any, chat_uuid: any): Promise<void>;
    onMessage(callback: any): Promise<void>;
    getLastMessages(): Promise<Messages>;
    getMoreMessages(): Promise<Messages>;
}
declare class Database {
    private firebaseApp;
    private chatRef;
    private newMessageListener;
    private firstTimestamp;
    constructor(config: ChatsCoreConfig);
    auth(config: ChatsCoreConfig): any;
    destroy(): any;
    setChatReference(path: string): void;
    setNewMessageListener(callback: (Message: any) => void): Promise<void>;
    unsetNewMessageListener(): Promise<void>;
    getLastMessages(amount?: number): Promise<Messages>;
    getMoreMessages(amount?: number): Promise<Messages>;
    private _updateFirstTimestamp;
}
export {};
