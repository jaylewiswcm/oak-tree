import { ConfigProperties } from '../types';
export declare class Config {
    data: ConfigProperties;
    update(config: Partial<ConfigProperties>): void;
    private _cleanData;
}
export declare const config: Config & ConfigProperties;
