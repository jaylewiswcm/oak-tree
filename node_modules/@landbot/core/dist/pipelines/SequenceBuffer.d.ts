import { Message } from '../types';
/**
 * A sequence buffer enqueues incoming messages by a sequence number and launches them
 * one by one when they're ordered.
 * Buffer is successfully dispatched when items follow an ordered sequence.
 */
export declare class SequenceBuffer {
    private $sequence;
    private currentSequenceIndex;
    state: Message[];
    constructor($sequence: any);
    maybeDispatchSequence(): void;
    push(item: Message): void;
    reset(): void;
    repeatedSequence(item: Message): boolean;
    repeatedSequenceZero(item: Message): boolean;
}
